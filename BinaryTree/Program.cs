using System;

namespace BinaryTree
{
    class Program
    {
        static void Main(string[] args)
        {
            BinaryITree tree = new BinaryITree();
            tree.Add(4);
            tree.Add(7);
            tree.Add(-5);
        }
    }
}
